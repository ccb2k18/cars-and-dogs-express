<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/grid-layout.css">
    <link rel="stylesheet" href="/styles/cars-gradient.css">
    <title>Cars</title>
</head>
<body>
    <div class="img-content">
        <% for (const carImageName of carImageNames) { %>
            <% console.log(carImageName); %> 
            <div>
                <img src="<%=`/images/${carImageName}`%>" alt="car"> 
            </div>
        <% } %> 
    </div>
</body>
</html>